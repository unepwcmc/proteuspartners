<%= render 'refinery/admin/search_header', :url => refinery.documents_admin_documents_path %>
<div class='pagination_container'>
  <% if @documents.any? %>
    <%= render 'documents' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
