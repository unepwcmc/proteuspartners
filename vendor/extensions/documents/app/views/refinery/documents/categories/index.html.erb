<% content_for :body do %>
  <ul>
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.title, refinery.documents_category_path(category) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
