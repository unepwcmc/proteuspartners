<%= render 'refinery/admin/search_header', :url => refinery.tools_admin_tools_path %>
<div class='pagination_container'>
  <% if @tools.any? %>
    <%= render 'tools' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
